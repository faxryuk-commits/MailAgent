# ✅ Проверка конфигурации

## Railway (Backend)

### Переменные окружения должны быть:
```
TELEGRAM_BOT_TOKEN=ваш_токен
OWNER_TELEGRAM_ID=ваш_id
OPENAI_API_KEY=ваш_ключ
WEB_ENABLED=true
WEB_PORT=$PORT          ← Важно: используйте $PORT, не конкретное число!
WEB_ACCESS_KEY=любая_строка (опционально)
```

### Проверьте:
- ✅ Сервис доступен по адресу: `https://mailagent-production-14c3.up.railway.app`
- ✅ В логах Railway должно быть: `🌐 Веб-интерфейс доступен на http://0.0.0.0:XXXX`
- ✅ API должен отвечать: `https://mailagent-production-14c3.up.railway.app/api/emails`

## Vercel (Frontend)

### Переменные окружения должны быть:
```
BACKEND_URL=https://mailagent-production-14c3.up.railway.app
WEB_ACCESS_KEY=та_же_строка_что_в_Railway (опционально)
OPENAI_API_KEY=ваш_ключ (опционально, если нужны AI функции на Vercel)
```

### Проверьте:
- ✅ `BACKEND_URL` указывает на ваш Railway домен (без `/api`, без порта)
- ✅ Домен должен начинаться с `https://`
- ✅ После деплоя Vercel должен успешно подключаться к Railway

## Тестирование

1. **Проверьте Railway API:**
   ```bash
   curl https://mailagent-production-14c3.up.railway.app/api/emails
   ```
   Должен вернуть JSON с письмами или пустой массив.

2. **Проверьте Vercel:**
   - Откройте ваш Vercel домен
   - Должна загрузиться главная страница
   - Если ошибка, проверьте логи в Vercel Dashboard → Logs

3. **Проверьте логи Railway:**
   - Railway Dashboard → MailAgent → Logs
   - Должны быть сообщения о запуске веб-интерфейса

## Частые проблемы

### Ошибка: "BACKEND_URL не настроен"
- ✅ Убедитесь, что в Vercel добавлена переменная `BACKEND_URL`
- ✅ Значение должно быть полным URL: `https://mailagent-production-14c3.up.railway.app`

### Ошибка: "Connection refused" или таймаут
- ✅ Проверьте, что на Railway `WEB_ENABLED=true`
- ✅ Проверьте, что Railway сервис запущен (зелёная галочка)
- ✅ Проверьте логи Railway на наличие ошибок

### Ошибка: "Python process exited with exit status: 1"
- ✅ Проверьте логи Vercel для деталей ошибки
- ✅ Убедитесь, что все зависимости установлены
- ✅ Проверьте, что `api/index.py` существует и правильно настроен

