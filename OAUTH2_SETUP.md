# 🔐 Пошаговая инструкция по настройке OAuth2 для Gmail

## Что нужно сделать вручную (я не могу это сделать за вас):

### Шаг 1: Создание проекта в Google Cloud Console

1. **Откройте** [Google Cloud Console](https://console.cloud.google.com/)
2. **Войдите** в свой Google аккаунт
3. **Создайте новый проект:**
   - Нажмите на выпадающий список проектов вверху (рядом с "Google Cloud")
   - Нажмите "New Project"
   - Введите название: `Mail Agent AI` (или любое другое)
   - Нажмите "Create"
   - Дождитесь создания проекта (5-10 секунд)

### Шаг 2: Включение Gmail API

1. В меню слева найдите **"APIs & Services"** → **"Library"**
2. В поиске введите: **"Gmail API"**
3. Нажмите на **"Gmail API"** в результатах
4. Нажмите кнопку **"Enable"** (Включить)
5. Дождитесь активации (несколько секунд)

### Шаг 3: Создание OAuth 2.0 Client ID

1. Перейдите в **"APIs & Services"** → **"Credentials"**
2. Нажмите **"+ CREATE CREDENTIALS"** вверху
3. Выберите **"OAuth client ID"**

4. **Если видите предупреждение "OAuth consent screen":**
   - Нажмите "CONFIGURE CONSENT SCREEN"
   - Выберите "External" (для личного использования)
   - Нажмите "CREATE"
   - Заполните форму:
     - **App name:** `Mail Agent AI`
     - **User support email:** ваш email
     - **Developer contact information:** ваш email
   - Нажмите "SAVE AND CONTINUE"
   - На странице "Scopes" нажмите "SAVE AND CONTINUE"
   - На странице "Test users" нажмите "SAVE AND CONTINUE"
   - На странице "Summary" нажмите "BACK TO DASHBOARD"

5. **Создание Client ID:**
   - Вернитесь в "Credentials"
   - Нажмите "+ CREATE CREDENTIALS" → "OAuth client ID"
   - **Application type:** выберите **"Web application"**
   - **Name:** `Mail Agent AI Bot`
   - **Authorized redirect URIs:** добавьте:
     ```
     http://localhost:8080/callback
     ```
     (Для Railway позже добавите ваш домен)
   - Нажмите **"CREATE"**

6. **Скопируйте данные:**
   - Появится окно с **Client ID** и **Client Secret**
   - **ВАЖНО:** Скопируйте оба значения!
   - Client ID выглядит примерно так: `123456789-abcdefghijklmnop.apps.googleusercontent.com`
   - Client Secret выглядит примерно так: `GOCSPX-abcdefghijklmnopqrstuvwxyz`

### Шаг 4: Добавление в Railway

1. Откройте ваш проект в Railway
2. Перейдите в **"Variables"**
3. Добавьте три переменные:

   ```
   GOOGLE_CLIENT_ID=ваш_client_id_здесь
   GOOGLE_CLIENT_SECRET=ваш_client_secret_здесь
   GOOGLE_REDIRECT_URI=http://localhost:8080/callback
   ```

4. Нажмите **"Deploy"** или Railway автоматически перезапустит

### Шаг 5: Использование в боте

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Выберите **"Аккаунт 1 — Gmail"** (или 2)
4. Введите ваш Gmail адрес
5. Бот отправит ссылку для авторизации
6. **Откройте ссылку в браузере**
7. **Войдите в Google** и разрешите доступ
8. После авторизации Google перенаправит на страницу с ошибкой (это нормально!)
9. **Скопируйте код из адресной строки:**
   - Найдите параметр `code=` в URL
   - Скопируйте весь код после `code=` до следующего `&`
   - Пример: `4/0AeanS...` (длинная строка)
10. **Отправьте код боту**
11. Готово! Аккаунт настроен

## ⚠️ Важные моменты:

- **Client Secret** - это секретная информация, не публикуйте её
- **Redirect URI** должен точно совпадать с тем, что указано в Google Cloud Console
- Если используете Railway, добавьте ваш домен в Authorized redirect URIs
- OAuth2 токены автоматически обновляются, не нужно их обновлять вручную

## 🔧 Если что-то не работает:

1. **Проверьте переменные окружения** в Railway
2. **Убедитесь, что Gmail API включен** в Google Cloud Console
3. **Проверьте Redirect URI** - должен совпадать везде
4. **Попробуйте использовать пароль** (если OAuth2 не работает)

## 📝 Альтернатива:

Если OAuth2 кажется сложным, можно использовать **App Password** (как было раньше):
- Просто не добавляйте `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET`
- Бот автоматически переключится на авторизацию по паролю

