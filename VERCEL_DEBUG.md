# 🔍 Диагностика ошибок Vercel

## Как посмотреть логи

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в раздел **Logs** (или **Deployments** → выберите последний деплой → **View Function Logs**)

## Что искать в логах

### ✅ Успешный запуск должен показать:
```
Python version: 3.11.x
Base directory: /var/task/...
✅ FastAPI app успешно импортирован
✅ Шаблоны загружены из: /var/task/app/templates
```

### ❌ Типичные ошибки:

#### 1. Ошибка импорта модуля
```
ModuleNotFoundError: No module named '...'
```
**Решение:**
- Проверьте, что все зависимости в `requirements.txt`
- Убедитесь, что Vercel использует правильный `requirements.txt`

#### 2. Ошибка пути к шаблонам
```
⚠️ Директория шаблонов не найдена
```
**Решение:**
- Проверьте, что `app/templates/` существует в репозитории
- Убедитесь, что файлы закоммичены в Git

#### 3. Ошибка импорта FastAPI
```
ImportError: cannot import name 'app' from 'app.web_app'
```
**Решение:**
- Проверьте, что `app/web_app.py` существует
- Убедитесь, что в файле есть `app = FastAPI(...)`

#### 4. Ошибка размера Lambda
```
Function size exceeds maximum allowed size
```
**Решение:**
- Убедитесь, что `vercel.json` содержит `maxLambdaSize: "50mb"`
- Проверьте, что не включаются лишние файлы

## Быстрая проверка

### 1. Проверьте структуру файлов:
```bash
ls -la api/
ls -la app/
ls -la app/templates/
```

Должны быть:
- ✅ `api/index.py`
- ✅ `app/web_app.py`
- ✅ `app/templates/index.html`
- ✅ `app/templates/email.html`
- ✅ `requirements.txt`

### 2. Проверьте переменные окружения в Vercel:
- ✅ `BACKEND_URL` = `https://mailagent-production-14c3.up.railway.app`
- ✅ `WEB_ACCESS_KEY` (опционально)

### 3. Проверьте Railway:
```bash
curl https://mailagent-production-14c3.up.railway.app/api/emails
```
Должен вернуть JSON (даже пустой массив `{"emails": []}`)

## Если ничего не помогает

1. **Скопируйте полные логи** из Vercel (все сообщения с ошибками)
2. **Проверьте последний коммит** - все файлы должны быть в репозитории
3. **Попробуйте пересоздать проект** на Vercel:
   - Удалите проект
   - Создайте заново
   - Подключите тот же репозиторий
   - Добавьте переменные окружения

## Альтернатива: Используйте Railway для веб-интерфейса

Если Vercel продолжает вызывать проблемы, можно использовать только Railway:

1. На Railway установите `WEB_ENABLED=true`
2. Откройте `https://mailagent-production-14c3.up.railway.app`
3. Веб-интерфейс будет работать напрямую на Railway

