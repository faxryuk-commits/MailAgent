# 🚀 Развёртывание веб-интерфейса на Vercel

## Архитектура

- **Vercel**: Веб-интерфейс (frontend)
- **Railway**: Backend (Telegram бот + проверка почты + API)

## Шаг 1: Настройка Railway (Backend)

1. В Railway создайте проект с типом **Web Service**
2. Добавьте переменные окружения:
   ```
   TELEGRAM_BOT_TOKEN=...
   OWNER_TELEGRAM_ID=...
   OPENAI_API_KEY=...
   WEB_ENABLED=true
   WEB_PORT=$PORT
   WEB_ACCESS_KEY=your-secret-key-here  # Опционально: любая строка для защиты API (можно не задавать)
   ```
3. Скопируйте URL вашего Railway сервиса (например: `https://your-app.railway.app`)

## Шаг 2: Развёртывание на Vercel

### Вариант A: Через Vercel CLI

1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Войдите в Vercel:
   ```bash
   vercel login
   ```

3. Деплой:
   ```bash
   vercel
   ```

4. Добавьте переменные окружения:
   ```bash
   vercel env add BACKEND_URL
   # Введите URL вашего Railway сервиса
   
   vercel env add WEB_ACCESS_KEY  # Опционально: любая строка (например: "my-secret-123")
   # Если не задать, защита будет отключена (подходит для личного использования)
   ```

### Вариант B: Через GitHub (рекомендуется)

1. Подключите репозиторий к Vercel:
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "Add New Project"
   - Выберите ваш GitHub репозиторий `MailAgent`

2. Настройки проекта:
   - **Framework Preset**: Other
   - **Root Directory**: `./` (корень проекта)
   - **Build Command**: (оставьте пустым)
   - **Output Directory**: (оставьте пустым)

3. Добавьте переменные окружения:
   - `BACKEND_URL` = URL вашего Railway сервиса (например: `https://your-app.railway.app`)
   - `WEB_ACCESS_KEY` = опционально: любая строка для защиты API (можно не задавать)
   - `OPENAI_API_KEY` = ваш ключ OpenAI (для AI функций в веб-интерфейсе)

4. Нажмите "Deploy"

## Шаг 3: Настройка API endpoints в Railway

В Railway добавьте API endpoint для получения данных:

1. Убедитесь, что `WEB_ENABLED=true`
2. Railway автоматически откроет веб-интерфейс на вашем домене
3. Vercel будет использовать этот URL как `BACKEND_URL`

## Альтернатива: Полностью на Vercel

Если хотите всё на Vercel, нужно:
1. Создать serverless функции для API
2. Использовать внешнюю БД для хранения данных
3. Настроить cron jobs для проверки почты

Но проще использовать гибридный подход: Vercel для UI, Railway для backend.

## Проверка работы

1. Откройте ваш Vercel домен
2. Должны увидеть список писем
3. При клике на письмо - полный текст и возможность ответить
4. Ответы отправляются через Railway backend

