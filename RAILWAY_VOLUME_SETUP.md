# 🔒 Настройка постоянного хранилища на Railway

## Проблема

По умолчанию файл `email_accounts.json` хранится в файловой системе контейнера. При каждом деплое Railway пересоздает контейнер, и все данные теряются. Это означает, что после каждого деплоя нужно заново настраивать почтовые аккаунты.

## Решение: Railway Volume

Railway Volume - это постоянное хранилище, которое сохраняется между деплоями.

## 📋 Инструкция по настройке

### Шаг 1: Создание Volume в Railway

1. Откройте ваш проект в Railway
2. Перейдите в раздел вашего сервиса (Worker)
3. Откройте вкладку **"Volumes"** (или **"Storage"**)
4. Нажмите **"New Volume"** или **"Add Volume"**
5. Укажите:
   - **Name**: `email-storage` (или любое другое имя)
   - **Mount Path**: `/data` (это стандартный путь, который использует приложение)
   - **Size**: 1 GB (достаточно для хранения аккаунтов)

### Шаг 2: Переменная окружения (опционально)

Если вы хотите использовать другой путь для Volume, добавьте переменную окружения:

- **Name**: `STORAGE_PATH`
- **Value**: `/data` (или ваш путь к Volume)

**Примечание**: Если переменная не задана, приложение автоматически использует `/data`.

### Шаг 3: Проверка

После настройки Volume:

1. Настройте почтовый аккаунт через бота
2. Сделайте деплой (push в GitHub)
3. Проверьте, что аккаунт остался настроенным после деплоя

## ✅ Результат

После настройки Volume:
- ✅ Данные аккаунтов сохраняются между деплоями
- ✅ Не нужно заново настраивать почту после каждого деплоя
- ✅ История писем сохраняется (если кэш также хранится в Volume)

## 🔍 Проверка работы

После настройки Volume проверьте логи при запуске:

**Если Volume настроен правильно:**
```
📁 Файл хранилища: /data/email_accounts.json
💾 Хранилище: /data
✅ Используется Railway Volume (данные сохраняются между деплоями)
✅ Загружено аккаунтов из /data/email_accounts.json: 1 (['1'])
```

**Если Volume НЕ настроен:**
```
📁 Файл хранилища: /app/email_accounts.json
⚠️  /data не найден, используем текущую директорию: /app
💾 Хранилище: /app
⚠️  Используется локальное хранилище: /app
   Для постоянного хранения на Railway настройте Volume (см. RAILWAY_VOLUME_SETUP.md)
```

**ВАЖНО:** Если вы видите путь `/app/email_accounts.json` вместо `/data/email_accounts.json`, значит Volume не настроен, и данные будут теряться при каждом деплое!

## ⚠️ Важно

- Volume создается один раз и сохраняется навсегда
- Данные в Volume не удаляются при деплое
- Если вы удалите Volume, все данные будут потеряны
- Рекомендуется делать резервные копии важных данных

## 🔄 Миграция существующих данных

Если у вас уже есть настроенные аккаунты:

1. Настройте Volume (см. выше)
2. Перезапустите сервис
3. Настройте аккаунты заново (они сохранятся в Volume)
4. После этого данные будут сохраняться между деплоями

## 📝 Альтернативные решения

Если Volume недоступен, можно использовать:

1. **Внешняя база данных** (PostgreSQL, MongoDB) - требует дополнительной настройки
2. **Облачное хранилище** (S3, Google Cloud Storage) - требует API ключи
3. **Переменные окружения** - не рекомендуется для паролей (ограничения по размеру)

Volume - самый простой и надежный вариант для Railway.

