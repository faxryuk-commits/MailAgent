# 🗄️ Настройка PostgreSQL на Railway (подробная инструкция)

## Зачем это нужно?

PostgreSQL - это база данных, которая будет хранить ваши почтовые аккаунты. Данные сохраняются автоматически и не теряются при деплое.

## 📋 Пошаговая инструкция

### Шаг 1: Откройте Railway Dashboard

1. Перейдите на https://railway.app
2. Войдите в свой аккаунт
3. Вы увидите список ваших проектов

### Шаг 2: Откройте ваш проект

1. Найдите проект **"accurate-education"** (или как называется ваш проект с MailAgent)
2. **Нажмите на название проекта** (не на сервис внутри, а на сам проект)
3. Вы попадете на страницу проекта

### Шаг 3: Добавьте PostgreSQL сервис

**Вариант A: Если видите кнопку "+ New" или "+ Add Service":**

1. В верхней части страницы проекта найдите кнопку:
   - **"+ New"** (обычно справа вверху)
   - Или **"+ Add Service"**
   - Или **"New"** с иконкой плюса
2. **Нажмите на эту кнопку**
3. Появится меню или список доступных сервисов

**Вариант B: Если видите вкладку "Services" или список сервисов:**

1. Посмотрите на страницу проекта
2. Там должен быть список сервисов (например, "MailAgent")
3. Рядом с этим списком должна быть кнопка **"+ New"** или **"Add Service"**
4. **Нажмите на неё**

### Шаг 4: Выберите PostgreSQL

После нажатия на "+ New" вы увидите список доступных сервисов:

1. Найдите в списке **"PostgreSQL"** (может быть с иконкой слона 🐘)
2. **Нажмите на "PostgreSQL"**
3. Railway автоматически:
   - Создаст новый сервис PostgreSQL
   - Настроит базу данных
   - Добавит переменные окружения для подключения

### Шаг 5: Дождитесь создания

1. Вы увидите, что Railway создает PostgreSQL сервис
2. Это займет 1-2 минуты
3. Когда готово, вы увидите новый сервис в списке (обычно называется "Postgres" или "PostgreSQL")

### Шаг 6: Проверьте переменные окружения

1. **Нажмите на сервис PostgreSQL** (который только что создали)
2. Перейдите на вкладку **"Variables"**
3. Вы увидите переменные:
   - `PGHOST` - хост базы данных
   - `PGPORT` - порт
   - `PGUSER` - пользователь
   - `PGPASSWORD` - пароль
   - `PGDATABASE` - имя базы данных
   - `DATABASE_URL` - полная строка подключения

**Эти переменные уже настроены автоматически!** Ничего менять не нужно.

### Шаг 7: Обновите код (я сделаю это)

После того, как вы добавите PostgreSQL, я обновлю код, чтобы он автоматически:
- Подключался к базе данных
- Создавал таблицы при первом запуске
- Сохранял аккаунты в базу данных
- Загружал аккаунты из базы данных

## ✅ Что будет после настройки

1. **Автоматическое сохранение**: Когда вы настраиваете аккаунт через бота, он автоматически сохраняется в PostgreSQL
2. **Данные не теряются**: При каждом деплое данные остаются в базе данных
3. **Не нужно ничего обновлять вручную**: Всё работает автоматически

## 🔍 Визуальная схема

```
Railway Dashboard
  └─ accurate-education (проект) ← НАЖМИТЕ СЮДА
      ├─ MailAgent (сервис)
      └─ + New (кнопка) ← НАЖМИТЕ СЮДА
          └─ Список сервисов:
              ├─ PostgreSQL ← ВЫБЕРИТЕ ЭТО
              ├─ MySQL
              ├─ MongoDB
              └─ ...
```

## ❓ Если не видите кнопку "+ New"

1. **Проверьте, что вы на странице проекта**, а не на странице сервиса
2. **Попробуйте обновить страницу** (F5 или Ctrl+R)
3. **Проверьте, что у вас есть права** на создание сервисов в проекте
4. **Попробуйте другой браузер** или режим инкогнито

## 📸 Что вы должны увидеть

После добавления PostgreSQL в списке сервисов будет:
- **MailAgent** (ваш основной сервис)
- **Postgres** или **PostgreSQL** (новый сервис базы данных)

## ⚠️ Важно

- PostgreSQL создается **бесплатно** на Railway (с ограничениями, но для вашего случая достаточно)
- База данных **автоматически** подключается к вашему сервису MailAgent
- **Ничего дополнительно настраивать не нужно** - Railway всё делает сам

## 🚀 После добавления PostgreSQL

Сообщите мне, когда добавите PostgreSQL, и я обновлю код для автоматической работы с базой данных!

