# 🔍 Проверка подключения к PostgreSQL

## Проблема

PostgreSQL работает (видно по логам), но нужно убедиться, что сервис MailAgent может к нему подключиться.

## ✅ Что нужно проверить

### 1. Переменная DATABASE_URL в сервисе MailAgent

Railway автоматически создает переменную `DATABASE_URL` для всех сервисов в проекте, когда добавляется PostgreSQL.

**Как проверить:**

1. Откройте сервис **MailAgent** (не PostgreSQL, а именно MailAgent)
2. Перейдите на вкладку **"Variables"**
3. Найдите переменную **`DATABASE_URL`**
4. Она должна содержать строку подключения вида:
   ```
   postgresql://postgres:password@host:5432/railway
   ```

**Если переменной нет:**

Railway должен был создать её автоматически, но иногда нужно:
1. Перезапустить сервис MailAgent
2. Или добавить переменную вручную (скопировать из сервиса PostgreSQL)

### 2. Проверка в логах MailAgent

После деплоя в логах MailAgent должно быть:

**Если PostgreSQL подключен:**
```
✅ Пул соединений с PostgreSQL создан
   Подключение к: host:5432
✅ Таблицы в PostgreSQL созданы/проверены
✅ PostgreSQL инициализирован и готов к работе
💾 Хранилище: PostgreSQL (данные сохраняются между деплоями)
```

**Если PostgreSQL НЕ подключен:**
```
⚠️  DATABASE_URL не найден, PostgreSQL недоступен
   Проверьте, что PostgreSQL добавлен в проект и переменные доступны
💾 Хранилище: /app
```

## 🔧 Решение проблем

### Проблема 1: DATABASE_URL не найден

**Решение:**
1. Откройте сервис PostgreSQL в Railway
2. Перейдите на вкладку **"Variables"**
3. Найдите переменную **`DATABASE_URL`**
4. Скопируйте её значение
5. Откройте сервис MailAgent
6. Перейдите на вкладку **"Variables"**
7. Нажмите **"+ New Variable"**
8. Name: `DATABASE_URL`
9. Value: вставьте скопированное значение
10. Сохраните

### Проблема 2: Ошибка подключения

**Проверьте:**
- PostgreSQL сервис запущен (зеленый статус)
- Переменная DATABASE_URL правильная
- Сервисы находятся в одном проекте Railway

## 📝 После исправления

1. Перезапустите сервис MailAgent (через вкладку "Deployments")
2. Проверьте логи - должно быть сообщение об успешном подключении
3. Настройте аккаунт через бота - данные сохранятся в PostgreSQL

